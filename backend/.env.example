# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (Change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-random-string

# MongoDB Configuration (Optional - app works without database using mock data)
MONGODB_URI=mongodb://your-mongodb-host:27017/airbnb-clone

# CORS Configuration
CORS_ORIGIN=https://demo.lambdatestinternal.com

# Optional: MongoDB Atlas Connection String
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/airbnb-clone?retryWrites=true&w=majority
